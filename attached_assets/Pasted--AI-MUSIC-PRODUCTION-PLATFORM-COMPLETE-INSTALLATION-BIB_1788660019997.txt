# AI MUSIC PRODUCTION PLATFORM

# COMPLETE INSTALLATION BIBLE

## Version: 2026-09-06

המטרה היא להביא את הפרויקט למצב שבו כל שכבת ה־AI/MIR/Generation/Rendering הנדרשת מותקנת באמת, אבל בלי לחזור לטעות של בניית infrastructure אינסופי במקום מוזיקה.

הסדר המחייב הוא:

**FIX ACTIVE MUSIC PATH**
→ **GOLDEN SONG BASELINE**
→ **FINISH EXISTING PROVIDERS**
→ **INSTALL MIR STACK**
→ **INSTALL GENERATION STACK**
→ **INSTALL SYMBOLIC RENDERING**
→ **FUSION**
→ **FULL-STACK GOLDEN SONG**
→ **10 REAL SONGS**

---

# 0. חוקי יסוד

## אין לשנות provider שכבר הוכח בלי סיבה

ACE-Step ו־BS-RoFormer שכבר עברו real inference הם baseline.

אין:

* upgrade אוטומטי
* dependency refresh
* checkpoint replacement
* runtime modernization

רק משום שקיימת גרסה חדשה יותר.

כל upgrade עתידי יהיה:

`NEW VERSION → isolated benchmark → compare → promote only if better`

---

# 1. Definition of Done

Provider נחשב `READY` רק כאשר מתקיימים כולם:

```text
SOURCE PINNED
↓
RUNTIME BUILT
↓
DEPENDENCIES PINNED
↓
WEIGHTS DOWNLOADED
↓
CHECKPOINT SHA VERIFIED
↓
REAL MODEL LOAD
↓
REAL INFERENCE
↓
REAL OUTPUT
↓
OUTPUT VALIDATED
↓
SMOKE PROOF PERSISTED
↓
DEPLOYED
↓
HEALTH VERIFIED
↓
NODE API CONNECTED
↓
CANONICAL OUTPUT VERIFIED
↓
SIGNED PROMOTION — GPU/Modal providers
↓
READY
```

לא להחשיב כ־READY:

```text
pip install succeeded
Docker build succeeded
import succeeded
checkpoint path exists
model loaded
endpoint responds
mock output
synthetic JSON
synthetic sine wave
```

---

# 2. Provider Isolation

אין להתקין את כל ה־AI stack באותו Python environment.

המבנה הרצוי:

```text
services/
  music-ai-worker/
      CPU / light MIR / rendering / DSP

  music-ai-gpu-worker/
      Dockerfile.ace-step
      Dockerfile.bs-roformer
      Dockerfile.mt3
      Dockerfile.all-in-one
      Dockerfile.musicgen
      Dockerfile.anyaccomp
      Dockerfile.symphonygen
      Dockerfile.meteor
      Dockerfile.midi-sag
```

כל משפחת מודלים עם runtime משלה.

---

# 3. ה־baseline שכבר קיים — אל תשבור אותו

ה־`model_manifest.json` הנוכחי כבר נועל את ארבעת המודלים הראשיים ואת ה־runtime identities שלהם. אין להחליף אותם תוך כדי העבודה.

## ACE_STEP

### Provider

```text
ACE_STEP
```

### Model

```text
ace-step-1.5-base
```

### Source revision שכבר נבדק

```text
ace-step/ACE-Step-1.5@
ca1e85fe9430179831e6bc6be790c332190a3866
```

### Runtime — לשמור בדיוק

```text
Python           3.11.11
CUDA             12.8.1
PyTorch          2.10.0+cu128
Torchvision      0.25.0+cu128
Torchaudio       2.10.0+cu128
Transformers     4.57.6
Accelerate       1.12.0
```

### GPU

```text
Modal L40S
```

### Checkpoint root

```text
/var/lib/music-ai-gpu/models/ace-step-1.5-runtime
```

### חובה לבדוק במבחני certification

```text
text2music
cover
repaint
lego
complete
extract — if supported by current runner
reference audio
full-song generation
```

### אין לבצע עכשיו

```text
uv sync latest
update ACE-Step
update Torch
replace checkpoint
switch to another ACE model
```

ה־upstream העדכני אמנם תומך Python 3.11–3.12, אך אנחנו שומרים על ה־runtime שכבר הוכח בפרויקט.

---

# 4. BS_ROFORMER

## Provider

```text
BS_ROFORMER
```

## Package baseline

```text
bs-roformer-infer==0.1.5
```

גם אם קיים upstream חדש יותר — לא לשדרג לפני Golden Song comparison.

## Model

```text
bs-roformer-viperx-v1
```

## Source

```text
puar-playground/bs-roformer@
b1361b816daca507f079d85e935c291bcb0a5351
```

## Checkpoint SHA

```text
5b84f37e8d444c8cb30c79d77f613a41c05868ff9c9ac6c7049c00aefae115aa
```

## Config SHA

```text
9df444dbc1a704e23858e0315a211ec5fa4c69f92ecefb173d05e8f721ed2b1f
```

## Runtime

```text
Python           3.11.11
CUDA             12.4.1
PyTorch          2.5.1+cu124
Torchvision      0.20.1+cu124
Torchaudio       2.5.1+cu124
Transformers     4.48.3
Accelerate       1.3.0
```

## GPU

```text
Modal L4
```

## Role

```text
PRIMARY SEPARATION
```

Output לפחות:

```text
VOCALS
INSTRUMENTAL
```

ואם המודל מחזיר:

```text
DRUMS
BASS
GUITAR
PIANO
OTHER
```

שמור גם אותם.

ה־upstream החדש יותר כולל lifecycle/checkpoint verification מתקדם, אבל אין צורך לשנות baseline מוכח לפני benchmark.

---

# 5. MT3

## Provider

```text
MT3
```

## Model

```text
mt3-ismir2021
```

## Adapter

```text
mt3-infer==0.1.3
```

## Adapter revision

```text
openmirlab/mt3-infer@
280a95817a67da0ae46987ddbb18c946963afffe
```

## Checkpoint source

```text
kunato/mt3-pytorch@
e203122fb40eefd3f9068dc6efd1870fe54ca57b
```

## Checkpoint SHA

```text
33f6bc4c0410a1c7c1c426c5406566de0b7418af2dc3dfd798496f70cef85622
```

## Original upstream

```text
magenta/mt3@
fa53e12321ac417d3baf01f43e6796a7d0775f55
```

## Runtime

```text
Python           3.11.11
CUDA             12.4.1
PyTorch          2.5.1+cu124
Transformers     4.38.2
absl-py          2.1.0
```

## מה לבצע עכשיו

לא להתקין מחדש.

לבצע:

```text
final Modal deploy
real L4 transcription
verify real note events
collect Modal IDs
signed promotion
worker identity Secret
rollover
publish API promotion
health
READY
```

---

# 6. ALL_IN_ONE

## Provider

```text
ALL_IN_ONE
```

## Package

```text
all-in-one-infer==3.1.0
```

3.1.0 הוא release הנוכחי שמחליף את madmom הישן ב־`madmom-infer`, בלי NATTEN חובה ועם התקנת PyPI רגילה.

## Runtime baseline

שמור את ה־runtime הנוכחי שכבר נבנה בפרויקט:

```text
Python       3.11.11
CUDA         12.4.1
PyTorch      2.5.1+cu124
```

לא צריך NATTEN.

Default:

```text
PURE PYTORCH ATTENTION
```

## Dependencies

```text
all-in-one-infer==3.1.0
demucs-infer==4.2.2
madmom-infer==0.2.0
```

## Harmonix folds

חובה לשמור את כל:

```text
fold0
fold1
fold2
fold3
fold4
fold5
fold6
fold7
```

ה־SHA המדויקים כבר רשומים ב־`model_manifest.json`.

## Demucs asset

```text
955717e8-8726e21a.th
```

SHA:

```text
8726e21a993978c7ba086d3872e7608d7d5bfca646ca4aca459ffda844faa8b4
```

## Aggregate model SHA

```text
4b8d00db3903c3b505cc5d2ec1a84787ccc2e1cc446f838118e03e9a356552e5
```

## עכשיו חסר

```text
REAL L4 GPU INFERENCE
SIGNED PROMOTION
READY
```

## Output חובה

```text
bpm
tempoMap
meter
meterMap
beats
downbeats
bars
section boundaries
section labels
confidence
```

All-In-One הוא כרגע provider החשוב ביותר ל־automatic structural understanding.

---

# 7. FIX ACTIVE ANALYSIS PATH

לפני provider חדש:

תקן את:

```text
analyzeProjectSource()
```

## Bass

חובה להכניס:

```ts
bass: providerResults.bassEvidence
```

ל־candidate הפעיל.

בנוסף:

```text
confidenceByField.bass
fieldStatus.bass
provenance.bass
providers.bass
validation
revision serialization
fusion
```

## מחק legacy

מחק לחלוטין:

```text
analyzeProjectSourceBeforeTask1()
```

ואם אין references אחרים:

```text
estimateKey(... fingerprint ...)
makeSections(...)
legacy fake beats
legacy meter=4/4 fallback
```

צריך להישאר **analysis path אחד בלבד**.

---

# 8. GOLDEN SONG BASELINE — לפני המשך ההתקנות

אחרי תיקון סעיף 7:

הרץ שיר אמיתי.

Flow:

```text
REAL SONG
→ BS_ROFORMER
→ SONG MODEL
→ HUMAN VERIFIED STRUCTURE if All-In-One not ready
→ ACE_STEP
→ 5 CANDIDATES
→ REAL WAVs
→ LISTEN
```

אין לחכות לשאר המודלים.

זה baseline להשוואה.

---

# 9. CPU / MIR WORKER — חבילת בסיס

בסביבת ה־light worker יש לשמור Python 3.11.

התקן/נעל:

```text
numpy==1.26.4
scipy==1.13.1
librosa==0.11.0
soundfile==0.13.1
mido==1.3.3
pretty-midi==0.2.11.post0
```

וכן:

```text
FFmpeg
libsndfile
```

FFmpeg הוא boundary הרשמי לכל conversion.

אין לכתוב codec logic חדש ב־Python אם FFmpeg עושה אותו טוב יותר.

---

# 10. BASIC PITCH

## Package

```bash
pip install basic-pitch==0.4.0
```

0.4.0 הוא ה־version הנוכחי של Spotify Basic Pitch, ותומך Python 3.11.

## Role

```text
SOLO / STEM TRANSCRIPTION
MT3 VERIFICATION
PITCH BEND EVIDENCE
```

## אל תשתמש בו כ־primary full-mix AMT

Use cases:

```text
piano stem
guitar stem
bass stem
clarinet
saxophone
violin
vocal stem
```

## Smoke fixtures

לפחות:

```text
real monophonic vocal
real piano
real bass
real solo instrument
```

Output canonical:

```text
pitch
start
end
velocity
pitchBend
confidence
provider
version
```

---

# 11. DEMUCS

ה־project root כבר משתמש ב:

```text
demucs==4.0.1
```

אל תשדרג כרגע.

Role:

```text
SECONDARY SEPARATION
BS_ROFORMER VERIFIER
```

Preload checkpoint בזמן bootstrap.

אסור model download בזמן API request.

Output:

```text
vocals
drums
bass
other
```

---

# 12. SHEETSAGE

## Package

```bash
pip install sheetsage-infer==0.2.1
```

## Exact release

```text
sheetsage-infer 0.2.1
```

Python:

```text
>=3.10
```

Use Python:

```text
3.11
```

Official release source revision:

```text
openmirlab/sheetsage-infer@
ee7c2aeeb8084840a4f938ae6913f566afdaebdc
```

PyPI wheel SHA:

```text
605ad15130587d548e40c3f4526aefd14748944003c462acb19d56149932bce4
```

## Role

```text
PRIMARY LEAD-SHEET / CHORD SEMANTICS
```

Return:

```text
melody
chord symbol
timing
confidence
```

Preserve exact chord meaning:

```text
C
Cm
Cmaj7
C7
Cm7
C/E
G7b9
C9#11
```

לא להפוך הכול ל־pitch-class triads.

## Model assets

Preload כל model asset ש־SheetSage דורש.

אם משתמשים ב־Jukebox profile:

הוא צריך runtime נפרד אם dependency stack שלו מתנגש.

Baseline ראשון יכול להיות handcrafted feature mode.

---

# 13. MADMOM-INFER

## Package

```bash
pip install madmom-infer==0.2.0
```

Python:

```text
>=3.9
```

Source license:

```text
BSD-2-Clause
```

אבל pretrained weights המקוריים של madmom הם:

```text
CC BY-NC-SA 4.0
```

## Important

`madmom-infer` normally supports verified on-demand weights.

אצלנו אין request-time downloads.

Bootstrap:

```text
create MadmomAnalyzer
→ load()
→ force all required checkpoints into cache
→ enumerate files
→ SHA-256 every file
→ persist manifest
→ copy/use durable asset volume
```

## Tasks

```text
beats
downbeats
tempo
key
onsets
chroma where available
```

Use one reusable session per worker rather than loading per request.

---

# 14. TORCHCREPE

## Package

```bash
pip install torchcrepe==0.0.24
```

Wheel SHA:

```text
ec054c23c9d45328f213f93a0131570a3f0e5903e9382792bed95f17a8c36d5a
```

Weights are packaged with the library.

## Models

```text
full
tiny
```

Default:

```text
full
```

## Output

```text
time
frequencyHz
periodicity
voiced
midiPitch
confidence
```

Use for:

```text
vocal
solo instrument
bass
melody validation
```

לא עבור dense full mix כ־truth source.

---

# 15. ESSENTIA

## Package

Use isolated MIR environment:

```bash
pip install essentia==2.1b6.dev1438
```

Current PyPI package is AGPL-3.0-only, therefore store that license explicitly and do not silently treat it as permissive just because the project is non-commercial.

## Role

```text
HPCP
CHROMA
KEY
SCALE
TONAL EVIDENCE
SPECTRAL EVIDENCE
ONSET / RHYTHM EVIDENCE
```

Do not install:

```text
essentia-tensorflow
```

unless a specific TensorFlow Essentia model becomes necessary.

---

# 16. CHROMA PROVIDER

זה custom provider, לא model עצמאי.

Build from:

```text
Essentia HPCP
+
librosa chroma
```

Pipeline:

```text
audio
→ chroma/HPCP
→ beat alignment
→ smoothing
→ pitch-class probability
→ candidate chords
```

Output:

```text
time
pitchClassProbabilities[12]
candidateChords
confidence
```

Use:

```text
HARMONY EVIDENCE ONLY
```

SheetSage remains semantic primary when reliable.

---

# 17. BASS PROVIDER

זה custom fusion provider.

Pipeline:

```text
BS_ROFORMER bass stem
         ↓
     TorchCREPE
         +
    Basic Pitch
         ↓
    BASS EVIDENCE
```

Output:

```text
start
end
pitch
pitchClass
confidence
sourceStemProvider
pitchProvider
```

זה חייב להגיע ל־active Song Model.

---

# 18. PYLOUDNORM

Install:

```bash
pip install pyloudnorm==0.2.0
```

Requires:

```text
Python >=3.9
```

0.2.0 מוסיף גם LRA measurement.

Run on:

```text
source
stems
generated candidates
mix
master
```

Store:

```text
integratedLUFS
loudnessRange
samplePeak
```

אל תקרא ל־sample peak:

```text
truePeak
```

אלא אם נבנה oversampled true-peak detector אמיתי.

---

# 19. MUSICGEN

## Dedicated environment only

Create:

```text
Dockerfile.musicgen
runners/musicgen.py
runners/requirements-musicgen.txt
```

## Code source

```text
facebookresearch/audiocraft@
896ec7c47f5e5d1e5aa1e4b260c4405328bf009d
```

## Runtime

```text
Python        3.9
PyTorch       2.1.0
FFmpeg        <5 preferred by upstream AudioCraft environment
```

AudioCraft explicitly documents Python 3.9 / PyTorch 2.1.0 and recommends FFmpeg below 5.

Do NOT merge this environment with ACE-Step.

## Models mandatory

```text
facebook/musicgen-large
facebook/musicgen-melody-large
```

Optional benchmark:

```text
facebook/musicgen-medium
facebook/musicgen-melody
```

MusicGen Large and Melody Large are 3.3B models.

## Model snapshot pins

Bootstrap from exact Hub revisions.

At current upstream baseline:

```text
musicgen-large → current immutable revision corresponding to 15ccdc9
musicgen-melody-large → 6fdf8d3
```

The bootstrap script MUST query the Hub once during provisioning, resolve the short revision to its full immutable commit SHA and then write the full 40-character SHA into `model_manifest.json`.

Never use `revision="main"` in production.

## GPU

```text
L40S
```

Do not deploy large on a GPU where it OOMs and silently switch model.

## Role

```text
ALTERNATIVE GENERATOR
TEXT CONDITIONING
MELODY CONDITIONING
```

Output:

```text
ProviderAudioArtifact
```

with:

```text
WAV
sampleRate
duration
seed
temperature
topK
topP
guidance
checkpoint SHA
artifact SHA
```

Weights license:

```text
CC-BY-NC 4.0
```

---

# 20. ANYACCOMP

## Code source

```text
AmphionTeam/AnyAccomp@
82604b5e3107944ad4c49fc64900b86118ae2c62
```

## Model source

```text
amphion/anyaccomp@
9aa9e62427337bf1df4caa3c4f3e6ad934522e71
```

Model snapshot is about 2.08 GB and contains VQ, Flow Matching and Vocoder components.

## Environment

```text
Python       3.9
FFmpeg       4.0
Torch        2.3.1
Torchaudio   2.3.1
Torchvision  0.18.1
Transformers 4.47.1
Accelerate   1.6.0
NumPy        1.26.4
Librosa      0.11.0
SciPy        1.13.1
SoundFile    0.13.1
```

אלה הגרסאות שה־requirements של ה־exact pinned commit דורשים.

## Files

```text
Dockerfile.anyaccomp
requirements-anyaccomp.txt
runners/anyaccomp.py
```

Vendor the exact upstream requirements into the provider-specific requirements file.

## Role

```text
VOCAL → ACCOMPANIMENT
SOLO INSTRUMENT → ACCOMPANIMENT
```

AnyAccomp תוכנן בדיוק לשני מקרי השימוש האלה.

## Model directories

```text
pretrained/vq
pretrained/flow_matching
pretrained/vocoder
```

## Smoke

לפחות:

```text
real clean vocal
real solo clarinet/violin/etc.
```

Output must be real accompaniment audio, not copied source.

---

# 21. SYMPHONYGEN

## Code

```text
symphonygen/symphonygen@
7a4e26f5b83bb7376f906b7f27bb298b3fb6bc94
```

## Runtime dependencies

Exact current upstream requirements:

```text
torch==2.8.0
torchaudio==2.8.0
transformers==5.1.0
mido
miditoolkit
soundfile
numpy
pandas
parse
tqdm
matplotlib
tensorboard
```

Use:

```text
Python 3.11
```

in an isolated image.

## Model source

```text
SymphonyGen/SymphonyGen
```

Pin Hub revision:

```text
e667241b7c347d89ddaa336f1b579a0fd24ce959
```

## Download all official checkpoints

```text
stage_one_pretrained.pt
stage_two_pretrained.pt
grpo_clamp_epoch_10.pt
grpo_clamp+track_epoch_6.pt
```

Current model repository is ~1.84 GB.

Primary production pair:

```text
stage_one_pretrained.pt
+
grpo_clamp+track_epoch_6.pt
```

## Flow

```text
harmony skeleton
→ orchestral generation
```

Also support:

```text
existing MIDI
→ --analyze_harmo
→ re-orchestration
```

## Output

Do not render immediately to generic audio only.

Convert:

```text
SymphonyGen MIDI
→ ArrangementPlan
→ TrackModels
```

Preserve:

```text
instrument
notes
velocity
timing
track
role
provenance
```

---

# 22. METEOR

## Code

```text
dinhviettoanle/meteor@
409d5b9684ac25ed9e65ead6fa15b3ac68460033
```

## Python

```text
3.9.2
```

Upstream explicitly specifies Python 3.9.2.

## Important runtime versions from exact requirements

```text
torch==2.3.0
numpy==1.26.4
scipy==1.13.0
mido==1.3.2
miditoolkit==1.0.1
miditok==3.0.3
pretty-midi==0.2.10
transformers-related stack from pinned requirements
```

Do not hand-upgrade these.

Use the complete pinned `requirements.txt` from commit `409d5...`.

## Install

```bash
pip install --no-deps -r requirements-meteor.txt
```

But first install the exact Torch/CUDA stack required by its environment.

## Model checkpoint

Download the official Hugging Face checkpoint referenced by the pinned upstream README/config.

During bootstrap:

```text
resolve exact HF repo
resolve immutable revision
download checkpoint
SHA-256
record repo + revision + filename + SHA
```

No floating `main`.

## Processing chain

Full support required:

```text
MIDI
→ midi_analyzed
→ corpus
→ events
→ MuseMorphose / REMI
→ attributes
→ METEOR
→ generated MIDI
```

The upstream explicitly defines this preprocessing chain.

## Controls

Expose:

```text
instrumentation
polyphonicity
rhythmicity
melody instrument
pitch range
repeatability / texture where supported
```

## Output

```text
MIDI
→ TrackModels
```

---

# 23. MIDI-SAG

## Code

```text
fundwotsai2001/MIDI-SAG@
b79839ed0cdd0b5e5f39d4cc4a80fcc90002d32f
```

## System

```text
Python 3.9
portaudio19-dev
cmake
```

## Exact bootstrap core

Upstream installation currently uses:

```text
pip<24.1
torch==2.5.1
torchaudio==2.5.1
pytorch-fast-transformers==0.4.0
transformers==4.47.1
huggingface-hub>=0.24.0,<1.0
torchcodec
openai-whisper
```

## Critical rule

DO NOT execute upstream `install.sh` blindly during production image startup.

Audit and reproduce everything it downloads in our checkpoint bootstrap.

The upstream downloads:

```text
RMVPE
GAME-1.0-medium
MuseControlLite
vocal beat detector
pretrained CSLL2M
SoulX-Singer
SoulX-Singer-Preprocess
```

All of those must become explicit manifest assets.

For each:

```text
source
revision / release
filename
size
SHA256
license
```

Google Drive assets have no immutable git revision, so the SHA of the exact downloaded bytes becomes the trust anchor.

## Modes

Required:

```text
Detected Mode
```

Flow:

```text
Vocal
→ Singing Vocal Beat Tracking
→ GAME
→ AccoMontage2
→ MuseControlLite
```

Ground-truth mode:

```text
Vocal + Vocal MIDI
→ AccoMontage2
→ MuseControlLite
```

ComposeFlow optional after first two work.

MIDI-SAG now documents both short and structure-aware full-song modes.

---

# 24. PEDALBOARD

Do not rebuild the integration.

Use the package version already resolved in the project lockfile.

Current root contract requires at least:

```text
pedalboard>=0.9.24
```

The existing worker is already real.

Use for:

```text
gain
EQ
compression
saturation
limiting
master processing
```

Only process final:

```text
MIX
MASTER
```

Do not master:

```text
STEM
PREMASTER
```

The export pipeline already verifies input/output SHA; preserve that behavior.

---

# 25. SFIZZ

Use:

```text
sfizz 1.2.3
```

Tag/release commit:

```text
4e70dc0
```

The upstream repository was archived in June 2026, so do not track floating HEAD. Pin release 1.2.3 permanently unless we deliberately migrate to another SFZ engine.

Build/install native:

```text
sfizz_render / native sfizz host
```

Do not use only a VST wrapper if native render is easier and deterministic.

---

# 26. VSCO 2 CE

Use:

```text
VSCO-2-CE 1.1.0 SFZ
```

Repository:

```text
sgossner/VSCO-2-CE
```

Use SFZ release/branch.

License:

```text
CC0-1.0
```

Store outside Git:

```text
/var/lib/music-ai/assets/sfz/vsco2-ce/
```

Calculate canonical tree SHA for entire library.

Manifest:

```text
name
version
source
treeSha256
sampleCount
bytes
license
```

---

# 27. VST3

VST3 cannot be marked READY merely because Pedalboard can load a plugin.

Need real native MIDI instrument host.

Create:

```text
services/music-ai-worker/native/vst3-render-host/
```

Contract:

```text
TrackModel JSON
+
plugin path
+
sample rate
+
render duration
→ WAV
```

Must support:

```text
note on/off
velocity
CC
pitch bend
program state
automation
```

A real instrument plugin must be supplied and licensed.

If none exists:

```text
VST3 = BLOCKED_MISSING_ASSET
```

Do NOT fake READY.

---

# 28. CANONICAL CPU LIBRARIES

In the common DSP/MIR environment pin:

```text
numpy==1.26.4
scipy==1.13.1
librosa==0.11.0
soundfile==0.13.1
mido==1.3.3
pretty-midi==0.2.11.post0
pyloudnorm==0.2.0
torchcrepe==0.0.24
basic-pitch==0.4.0
sheetsage-infer==0.2.1
madmom-infer==0.2.0
```

Essentia can remain in a separate environment because of packaging/license differences:

```text
essentia==2.1b6.dev1438
```

Do not force one dependency resolver to reconcile every research model.

---

# 29. PROVIDER REGISTRY

Final analysis providers:

```text
BS_ROFORMER
DEMUCS
ALL_IN_ONE
MT3
BASIC_PITCH
SHEETSAGE
MADMOM
TORCHCREPE
ESSENTIA
CHROMA
BASS
LOUDNESS
```

Final generation providers:

```text
ACE_STEP
MUSICGEN
ANYACCOMP
MIDI_SAG
METEOR
SYMPHONYGEN
```

Rendering:

```text
PEDALBOARD_BUILTIN
SFIZZ_VSCO2_CE
VST3
```

---

# 30. PROVIDER ROUTING

Do not ask every provider to solve every problem.

## Separation

```text
BS_ROFORMER
  primary

DEMUCS
  verifier / fallback
```

## Structure

```text
ALL_IN_ONE
  primary

MADMOM
  secondary beat/downbeat evidence
```

## Polyphonic transcription

```text
MT3
  primary
```

## Solo / stem notes

```text
BASIC_PITCH
```

## Continuous F0

```text
TORCHCREPE
```

## Harmony semantics

```text
SHEETSAGE
```

## Harmony evidence

```text
CHROMA
BASS
ESSENTIA
```

## Full audio generation

```text
ACE_STEP
```

## Solo/vocal accompaniment

```text
ANYACCOMP
```

## Alternate text/melody generation

```text
MUSICGEN
```

## Vocal-conditioned song pipeline

```text
MIDI_SAG
```

## Re-orchestration

```text
METEOR
```

## Orchestral symbolic generation

```text
SYMPHONYGEN
```

---

# 31. ANALYSIS FUSION

Implement true per-field fusion.

## Tempo

```text
All-In-One
Madmom
local estimate
```

Detect half/double BPM:

```text
70 ↔ 140
80 ↔ 160
```

## Meter

```text
All-In-One
Madmom downbeats
MIDI metadata
```

No fake 4/4.

## Key

```text
Essentia
Madmom
local key evidence
SheetSage chord context
```

## Melody

```text
MT3
Basic Pitch
TorchCREPE
SheetSage
```

## Harmony

```text
SheetSage
Chroma
Bass
Melody conflict checking
Key
```

## Structure

```text
All-In-One
```

If unavailable:

```text
not_available
```

not generated Section 1 / Section 2.

---

# 32. SONG MODEL

Final canonical model must include:

```text
audio
tempoMap
meterMap
keyMap

beats
downbeats
bars

sections

melody
bass
chords

energy
dynamics

waveform

stems
sourceStems

lyrics

confidenceByField
fieldStatus
providerProvenance
fusion decisions
```

For each field:

```text
status
confidence
providers
message
edited
```

Allowed statuses:

```text
detected
low_confidence
not_available
human_verified
```

---

# 33. HARMONY ENGINE

Current improvements stay.

Expand candidate scoring to:

```text
melody fit
bass fit
key fit
voice leading
functional progression
cadence
section role
harmonic rhythm
style
```

Support:

```text
major
minor
dominant
maj7
min7
dim
aug
sus
6
9
11
13
alterations
slash chords
```

Advanced harmony only when requested:

```text
secondary dominant
borrowed chord
modal interchange
passing diminished
tonicization
```

No random sophistication.

---

# 34. ARRANGEMENT DIRECTOR

Each section must generate:

```text
active instruments
inactive instruments

energy
density

register
rhythmic activity
harmonic activity

dynamic target
articulation

countermelody policy
fill policy

entry
exit
transition
```

Global arc:

```text
intro
verse
pre-chorus
chorus
verse 2
chorus 2
bridge
final chorus
outro
```

The Director must create real changes in notes/audio, not only metadata.

---

# 35. MODEL ASSET STORAGE

Use durable model volume:

```text
music-ai-models-v1
```

Jobs:

```text
music-ai-jobs-v1
```

Outputs:

```text
music-ai-outputs-v1
```

Production remains:

```python
create_if_missing=False
```

---

# 36. CHECKPOINT BOOTSTRAP

Extend `checkpoint_bootstrap.py`.

For every model:

```text
download only during bootstrap
↓
immutable revision
↓
temporary directory
↓
size check
↓
file SHA
↓
canonical directory SHA
↓
atomic publish
↓
readiness marker
↓
volume commit
```

No model is allowed to call Hugging Face/GitHub/Google Drive during a user request.

---

# 37. MANIFEST

Every model record needs:

```text
provider
modelVersion

codeRepository
codeRevision

modelRepository
modelRevision

checkpointPaths
checkpointSha256

aggregateSha256

runtime:
  python
  cuda
  torch
  torchaudio
  torchvision
  transformers
  accelerate

license:
  source
  weights

runner
smoke
gpu
```

No null SHA for READY provider.

---

# 38. MODAL APP

After each provider independently passes local/container smoke, expand:

```text
modal_app.py
modal_config.py
```

to include:

```text
ACE_STEP
BS_ROFORMER
MT3
ALL_IN_ONE
MUSICGEN
ANYACCOMP
SYMPHONYGEN
METEOR
MIDI_SAG
```

Every provider:

```text
unique Dockerfile
unique modal.Image
max_inputs=1 initially
appropriate GPU
own promotion Secret
```

---

# 39. GPU DEFAULTS

Use initial deployment choices:

```text
ACE_STEP       L40S
BS_ROFORMER    L4
MT3            L4
ALL_IN_ONE     L4
MUSICGEN       L40S
ANYACCOMP      L40S first; reduce only after profiling
SYMPHONYGEN    L4/L40S benchmark, start L40S
METEOR         L4
MIDI_SAG       L40S first because of multi-model pipeline
```

Do not optimize cost yet.

First prove correct inference.

---

# 40. HEALTH CONTRACT

`GET /health?provider=X`

must include:

```text
provider
modelVersion

runtimeReady
packageReady
checkpointReady
checkpointSha256

smokeTested
smokeTimestamp

sourceRevision
imageDigest

gpu

healthy
status
```

For promoted providers additionally:

```text
modalAppId
modalDeploymentId
modalFunctionId
modalImageId
promotionValid
```

---

# 41. SMOKE = REAL INFERENCE

Examples:

## MT3

```text
32s real/polyphonic audio
→ notes > 0
```

## All-In-One

```text
real song
→ bpm + downbeats + sections
```

## MusicGen

```text
text prompt
→ non-silent generated WAV
```

and:

```text
melody conditioning
→ non-silent WAV influenced by input melody
```

## AnyAccomp

```text
real vocal
→ accompaniment
```

## SymphonyGen

```text
harmony skeleton
→ multitrack MIDI
```

## METEOR

```text
real MIDI
→ new MIDI
```

## MIDI-SAG

```text
real vocal
→ backing output
```

---

# 42. GOLDEN SONG — ROUND 1

Do this BEFORE finishing the full installation stack.

Required outputs:

```text
source.wav

song-model.json

candidate-01.wav
candidate-02.wav
candidate-03.wav
candidate-04.wav
candidate-05.wav

candidate-evaluation.json
```

All 5 candidates from ACE-Step.

Listen manually.

Store human scores.

---

# 43. GOLDEN SONG — ROUND 2

After:

```text
MT3
All-In-One
SheetSage
Madmom
TorchCREPE
Essentia
Bass
Chroma
```

are active, rerun same song.

Compare Song Model:

```text
tempo
meter
key
sections
melody
chords
bass
```

against Round 1.

---

# 44. GOLDEN SONG — GENERATOR SHOOTOUT

Then generate candidate families from:

```text
ACE_STEP
ANYACCOMP
MUSICGEN
MIDI_SAG
METEOR/SYMPHONYGEN where input type fits
```

Do NOT put every provider in one ranking pool without provider/type metadata.

Store:

```text
provider
candidateId
seed
generation type
audio or symbolic
human rating
```

---

# 45. HUMAN QUALITY FIELDS

I want manual rating for:

```text
melody preservation       1–10
harmony                    1–10
groove                     1–10
instrument choices         1–10
section development        1–10
transitions                1–10
musical taste              1–10
audio quality              1–10
emotional fit              1–10
overall                    1–10
```

plus:

```text
would_use_in_real_production = yes/no
```

---

# 46. AUTOMATIC CANDIDATE QA

Hard reject:

```text
silence
corrupt WAV
wrong duration
severe clipping
missing lineage
missing source
invalid RIFF
checksum mismatch
```

Soft score:

```text
melody similarity
harmony agreement
structural alignment
section contrast
bass harmony consistency
loudness
spectral balance
transition continuity
```

Do not present these metrics as “musical taste”.

---

# 47. TEN SONG TEST

After one Golden Song works:

Use 10 real songs.

Include:

```text
major
minor
ballad
upbeat
3/4 or 6/8
dense mix
sparse mix
vocal
solo instrument
orchestral / cinematic
```

Per song:

```text
analysis
5 candidates
select best
1–2 targeted regenerations
final export
human rating
```

Target:

```text
6–7 / 10
```

must produce something genuinely usable or meaningfully developable in a real production.

Not merely:

```text
“good for AI”
```

---

# 48. FINAL READY MATRIX

The full project should ultimately report a truthful status for:

```text
ACE_STEP
BS_ROFORMER
MT3
ALL_IN_ONE

BASIC_PITCH
DEMUCS
SHEETSAGE
MADMOM
TORCHCREPE
ESSENTIA
CHROMA
BASS
PYLOUDNORM

MUSICGEN
ANYACCOMP
SYMPHONYGEN
METEOR
MIDI_SAG

PEDALBOARD_BUILTIN
SFIZZ_VSCO2_CE
VST3
```

Statuses allowed:

```text
READY
BLOCKED
FAILED
UNCONFIGURED
```

No fake green.

---

# 49. EXACT PRIORITY FROM THIS MOMENT

Do these tasks in this exact order.

## Task 1

```text
wire bassEvidence into active analyzeProjectSource()
```

## Task 2

```text
delete analyzeProjectSourceBeforeTask1()
delete legacy analysis helpers with no references
```

## Task 3

```text
tests
```

## Task 4

```text
Golden Song baseline
ACE-Step
5 WAV candidates
```

## Task 5

```text
finish MT3 promotion
```

## Task 6

```text
finish All-In-One real GPU smoke + promotion
```

## Task 7

Install/activate MIR stack:

```text
SheetSage 0.2.1
Madmom-Infer 0.2.0
TorchCREPE 0.0.24
Essentia
Chroma
Bass
pyloudnorm 0.2.0
```

## Task 8

Rerun Golden Song analysis.

## Task 9

Install AnyAccomp.

## Task 10

Install MusicGen.

## Task 11

Install MIDI-SAG.

## Task 12

Run audio-generator comparison.

## Task 13

Install METEOR.

## Task 14

Install SymphonyGen.

## Task 15

Install:

```text
sfizz 1.2.3
VSCO2 CE 1.1.0
```

## Task 16

VST3 host + actual licensed plugin asset.

## Task 17

Full-stack Golden Song.

## Task 18

10-song test.

---

# 50. ABSOLUTELY DO NOT DO

Until Golden Song Round 1 is completed:

```text
DO NOT add more export monitoring
DO NOT add storage abstractions
DO NOT refactor generic job infrastructure
DO NOT create another dashboard
DO NOT improve the sine-wave renderer
DO NOT optimize cloud costs
DO NOT upgrade working ACE-Step
DO NOT upgrade working BS-RoFormer
```

From this moment:

**MUSICAL OUTPUT IS THE PRIMARY DELIVERABLE.**

---

# FINAL DEFINITION OF SUCCESS

The system is not successful because:

```text
21 providers are green
```

The system is successful when:

```text
I upload a real song
↓
the system understands its structure, rhythm, melody and harmony
↓
it creates multiple genuinely different arrangements
↓
the arrangements preserve the identity of the song
↓
sections develop musically
↓
instruments behave intentionally
↓
I can regenerate a weak section/instrument
↓
I can select a candidate
↓
I receive real WAV/stems/MIDI
↓
the result is useful in a real music-production workflow
```

All implementation decisions from this point onward must be judged against that objective.
