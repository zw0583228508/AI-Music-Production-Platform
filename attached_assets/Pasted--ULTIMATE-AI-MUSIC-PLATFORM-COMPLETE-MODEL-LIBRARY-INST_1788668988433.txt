# ULTIMATE AI MUSIC PLATFORM

# COMPLETE MODEL & LIBRARY INSTALLATION MASTER PLAN

## Objective

המטרה של השלב הזה היא לסיים את **כל שכבת ההתקנות** של הפרויקט ברמה הגבוהה ביותר האפשרית, כולל:

* MIR
* source separation
* transcription
* structure
* harmony
* musical reasoning
* vocal-to-accompaniment
* full-song generation
* symbolic arrangement
* controllable neural rendering
* region repair/inpainting
* open-vocabulary separation
* candidate evaluation
* quality ranking
* real-time generation
* symbolic rendering
* mastering

בשלב הזה אין לבצע עדיין Golden Song evaluation או שיפורי Arrangement Director.

בסיום המשימה צריכה להיות מערכת שבה כל capability שידוע לנו שאנחנו רוצים כבר:

```text
INSTALLED
OR
RESEARCH_ONLY_INSTALLED
OR
BLOCKED_LICENSE
OR
BLOCKED_NO_WEIGHTS
```

אין status אחר.

---

# 1. ABSOLUTE ARCHITECTURE RULE

אין להתקין את כל המחקר באותו Python environment.

יש לשמור provider isolation.

מבנה מומלץ:

```text
services/
  music-ai-worker/
  music-ai-gpu-worker/
  music-mir-worker/
  sheetsage-worker/

  moss-music-worker/
  songformer-worker/
  stable-audio3-worker/
  lada-band-worker/
  hafm-worker/
  magenta-rt2-worker/
  sam-audio-worker/
  muq-eval-worker/
  song-eval-worker/
  song-aesthetics-worker/
  music2music-worker/
  diffrhythm2-worker/
```

אם provider כבר חי בתוך worker קיים בלי dependency conflict — אין ליצור worker נוסף רק ליופי.

---

# 2. DO NOT REINSTALL WORKING PROVIDERS

Freeze את:

```text
ACE_STEP
BS_ROFORMER
MT3_BASELINE
ALL_IN_ONE
BASIC_PITCH
DEMUCS
MADMOM
TORCHCREPE
ESSENTIA
CHROMA
PYLOUDNORM
PEDALBOARD_BUILTIN
```

לא לשדרג runtime שלהם תוך כדי המשימה.

---

# 3. EXISTING BASE STACK — FINALIZE ONLY

## ACE-Step

שמור deployment/promotion/checkpoint הנוכחיים.

Provider:

```text
ACE_STEP
```

Capabilities:

```text
FULL_GENERATION
REFERENCE_AUDIO
COVER
REPAINT
LEGO
COMPLETE
```

לא לשנות Torch/CUDA.

---

## BS-RoFormer

Provider:

```text
BS_ROFORMER
```

Primary separation.

לא להחליף checkpoint.

---

## MT3

Provider baseline:

```text
MT3_PYTORCH
```

שמור את המודל הקיים.

בהמשך נוסיף שני backends נוספים תחת אותה משפחה.

---

## All-In-One

להשאיר מותקן כרגע.

Provider:

```text
ALL_IN_ONE
```

גם אם בעתיד SongFormer ינצח אותו ב־structure, אין להסיר אותו בשלב ההתקנה.

---

# 4. FINISH SHEETSAGE

Provider:

```text
SHEETSAGE
```

שמור:

```text
sheetsage-infer==0.2.1
Python 3.11
HANDCRAFTED profile
```

חובה להשלים:

```text
license secret
all model assets
all Madmom downbeat assets
asset SHA validation
durable volumes
Modal deployment
API endpoint
```

Secret:

```text
sheetsage-noncommercial-license-v1
```

חייב לספק:

```text
SHEETSAGE_ACCEPT_NONCOMMERCIAL_WEIGHTS=CC-BY-NC-SA-3.0+4.0
```

לא להפעיל Jukebox profile כ־default.

---

# 5. INSTALL BEAT THIS!

זה provider חדש ומומלץ.

Provider ID:

```text
BEAT_THIS
```

Install:

```bash
pip install beat-this==1.1.0
```

Reference dependencies:

```text
torch>=2
torchaudio
einops
rotary-embedding-torch
soxr
numpy
ffmpeg
```

Code + published weights:

```text
MIT
```

Create:

```text
services/music-mir-worker/providers/beat_this.py
```

או worker מבודד אם Torch environment מתנגש.

Pre-download model:

```text
final0
```

אסור:

```text
torch.hub download בזמן request
```

Bootstrap אותו מראש.

Canonical output:

```json
{
  "provider": "BEAT_THIS",
  "beats": [],
  "downbeats": [],
  "confidence": 0.0
}
```

Role:

```text
PRIMARY_DEDICATED_BEAT_TRACKER
```

All-In-One/Madmom הופכים secondary evidence.

---

# 6. INSTALL SONGFORMER

Provider:

```text
SONGFORMER
```

Worker:

```text
services/songformer-worker/
```

Runtime:

```text
Python 3.10
Ubuntu 22.04-compatible image
```

Source:

```text
ASLP-lab/SongFormer
```

ב־bootstrap:

```bash
git clone <pinned revision>
git submodule update --init --recursive
pip install -r requirements.txt
```

יש להשתמש ב־exact immutable commit, לא `main`.

Download assets מראש:

```text
SongFormer.safetensors
MuQ
MusicFM
MusicFM msd_stats
all required configs
```

SongFormer עצמו משתמש ב־MuQ וב־MusicFM representations.

HF model:

```text
ASLP-lab/SongFormer
```

כיום repository מלא הוא בערך 3GB, כולל model ו־support assets.

Canonical output:

```text
section boundaries
section labels
confidence
section embeddings if available
```

Role:

```text
PRIMARY_STRUCTURE_CANDIDATE
```

לא למחוק All-In-One עדיין.

---

# 7. INSTALL MOSS-MUSIC — VERY HIGH PRIORITY

זה provider חדש מסוג אחר לגמרי.

Provider IDs:

```text
MOSS_MUSIC_INSTRUCT
MOSS_MUSIC_THINKING
```

Worker:

```text
services/moss-music-worker/
```

Source:

```text
OpenMOSS/MOSS-Music
```

Models:

```text
OpenMOSS-Team/MOSS-Music-8B-Instruct
OpenMOSS-Team/MOSS-Music-8B-Thinking
```

License:

```text
Apache-2.0
```

Runtime reference:

```text
Python 3.12
CUDA 12.8
Torch runtime from exact pinned MOSS repo
FFmpeg 7
```

Official install baseline:

```bash
conda create -n moss-music python=3.12
conda install -c conda-forge "ffmpeg=7"
pip install --extra-index-url https://download.pytorch.org/whl/cu128 -e ".[torch-runtime]"
```

Preferred serving:

```text
SGLang
```

Official project recommends SGLang for best quality/throughput.

Install SGLang inside isolated image only.

Pre-download:

```bash
hf download OpenMOSS-Team/MOSS-Music-8B-Instruct
hf download OpenMOSS-Team/MOSS-Music-8B-Thinking
```

Resolve exact HF commit SHA for both.

Do not use community MLX quantization in server production; keep it as optional Mac/dev profile.

Capabilities:

```text
music captioning
genre/style reasoning
lyrics ASR
structural reasoning
chord/key/tempo reasoning
instrumentation analysis
long-form musical QA
arrangement critique
semantic song explanation
```

Important:

MOSS is NOT canonical truth.

MOSS output must enter:

```text
MUSICAL_SEMANTIC_REASONING
```

and never silently replace Essentia/MT3/SongFormer facts.

---

# 8. INSTALL LaDA-BAND

Provider:

```text
LADA_BAND
```

Worker:

```text
services/lada-band-worker/
```

Source:

```text
Duoluoluos/TME-LaDA-Band
```

Weights:

```text
sDuoluoluos/LaDA-Band
```

Access is gated.

Current license/status:

```text
NONCOMMERCIAL_RESEARCH_ONLY
```

HF gate explicitly requires non-commercial research use.

Therefore provider status:

```text
RESEARCH_ONLY
```

not production-commercial.

### Installation

1. Request/confirm HF access.
2. Store HF access token only in Secret.
3. Clone exact pinned TME-LaDA-Band revision.
4. Inspect its current environment file.
5. Build provider-specific image.
6. Do not merge with ACE-Step.

Create:

```text
model_manifest.json
license_manifest.json
bootstrap_assets.py
app.py
inference.py
```

Download entire required:

```text
checkpoints/
pretrained/
```

tree from gated HF repo.

Calculate canonical tree SHA.

Input contract:

```text
dry vocal audio
optional text/style prompt
```

Output:

```text
accompaniment WAV
```

Provider role:

```text
PRIMARY_DIRECT_V2A_RESEARCH_CANDIDATE
```

---

# 9. INSTALL HAFM

Provider:

```text
HAFM
```

Worker:

```text
services/hafm-worker/
```

Source:

```text
HackerHyper/HAFM
```

Model:

```text
zhuqijian/HAFM
```

License:

```text
Apache-2.0
```

HAFM already exposes inference such as:

```bash
python infer_simple.py \
  --vocal_path vocal.wav \
  --output_path output.wav \
  --config configs/ar.yaml
```

Build exact environment from pinned upstream.

Do not invent dependency versions if upstream pins them.

Architecture assets include support for:

```text
HuBERT semantic representation
EnCodec acoustic representation
hierarchical autoregressive model
```

Input:

```text
isolated singing vocal
```

Output:

```text
instrumental accompaniment WAV
```

Provider role:

```text
V2A_ALTERNATIVE
```

---

# 10. KEEP ANYACCOMP AND FINISH IT

Provider:

```text
ANYACCOMP
```

Already planned.

Isolated:

```text
Python 3.9
Torch 2.3.1
Torchaudio 2.3.1
Torchvision 0.18.1
Transformers 4.47.1
Accelerate 1.6.0
```

Download complete:

```text
amphion/anyaccomp
```

including:

```text
VQ
Flow Matching
Vocoder
```

Now the V2A family becomes:

```text
LADA_BAND
HAFM
ANYACCOMP
MIDI_SAG
```

---

# 11. FINISH MIDI-SAG + MUSECONTROLLITE

Provider:

```text
MIDI_SAG
```

This is not optional anymore.

Worker:

```text
services/midi-sag-worker/
```

Runtime:

```text
Python 3.9
Torch 2.5.1
Torchaudio 2.5.1
Transformers 4.47.1
pytorch-fast-transformers 0.4.0
```

System:

```text
portaudio19-dev
cmake
ffmpeg
libsndfile
```

Do not execute `install.sh` blindly in production.

Convert all downloads into manifest-controlled assets:

```text
RMVPE
GAME-1.0-medium
MuseControlLite
Vocal Beat Tracking
AccoMontage2 assets
pretrained CSLL2M
SoulX-Singer
SoulX-Singer-Preprocess
```

Pipeline required:

```text
Vocal
→ Beat Tracking
→ GAME MIDI
→ AccoMontage2 chords
→ MuseControlLite
→ backing audio
```

This is the official detected-mode architecture.

### Important architectural work during installation

Expose MuseControlLite separately as a capability:

```text
MUSE_CONTROL_LITE
```

even if packaged inside MIDI-SAG worker.

This will later allow our SongModel to directly feed:

```text
melody
chroma/harmony
rhythm
dynamics
reference audio
```

into neural generation.

---

# 12. INSTALL STABLE AUDIO 3

Provider family:

```text
STABLE_AUDIO_3_SMALL_MUSIC
STABLE_AUDIO_3_MEDIUM
```

Worker:

```text
services/stable-audio3-worker/
```

Source:

```text
Stability-AI/stable-audio-3
```

Runtime from current reference:

```text
Python >=3.10
torch==2.7.1
torchaudio==2.7.1
transformers>=5.8.0
numpy>=2.2.6
safetensors>=0.7
huggingface-hub>=1.7
soundfile>=0.13.1
```

Install with pinned repository:

```bash
uv sync
```

Do not float `main`.

Models:

```text
stabilityai/stable-audio-3-small-music
stabilityai/stable-audio-3-medium
```

Small model weights are about 2.27GB; Medium model weights about 9.22GB.

Both are gated by Stability license acceptance.

Capabilities:

```text
TEXT_TO_AUDIO
AUDIO_TO_AUDIO
INPAINT
CONTINUE
LORA
```

CLI/API must expose:

```text
prompt
duration
initAudio
initNoiseLevel
inpaintStart
inpaintEnd
loraPath
loraStrength
```

Stable Audio 3 becomes:

```text
SECTION_REPAIR_ENGINE_2
```

alongside ACE-Step Repaint.

---

# 13. INSTALL MAGENTA REALTIME 2

Provider family:

```text
MAGENTA_RT2_SMALL
MAGENTA_RT2_BASE
```

Worker:

```text
services/magenta-rt2-worker/
```

Source:

```text
magenta/magenta-realtime
```

Runtime:

```text
Python 3.12
```

Linux/NVIDIA:

```bash
uv pip install "magenta-rt" "jax[cuda13]"
```

Official model bootstrap:

```bash
mrt models init
mrt models download
```

Download:

```text
MusicCoCa
SpectroStream
mrt2_small
mrt2_base
```

Model sizes:

```text
small = 230M
base  = 2.4B
```

License:

```text
code: Apache-2.0
weights: CC-BY-4.0
```

Use cases:

```text
offline neural rendering
MIDI-conditioned performance
style-conditioned performance
audio-context continuation
future live accompaniment
future DAW integration
```

Important:

Do NOT present MRT2 as deterministic MIDI renderer.

Provider metadata:

```text
renderingMode = GENERATIVE_MIDI_CONDITIONED
```

SFZ/VST3 remains:

```text
DETERMINISTIC_MIDI_RENDERING
```

---

# 14. INSTALL SAM AUDIO

Provider:

```text
SAM_AUDIO
```

Worker:

```text
services/sam-audio-worker/
```

Source:

```text
facebookresearch/sam-audio
```

Requirements:

```text
Python >=3.11
CUDA recommended
```

Official install:

```bash
pip install .
```

Current dependencies include:

```text
dacvae
audiobox_aesthetics
einops
ImageBind
laion-clap
numpy
perception-models
pydub
torch
torchaudio
torchcodec
torchdiffeq
torchvision
transformers>=4.54
```

Important:

SAM Audio has nontrivial dependency conflicts in the wild.

Therefore:

* dedicated Docker image only;
* pin exact tested commit;
* produce lockfile;
* never share env with MOSS/Stable Audio/MIR.

Models to request/access:

```text
sam-audio-small
sam-audio-base
sam-audio-large
```

Optional TV variants:

```text
sam-audio-small-tv
sam-audio-base-tv
sam-audio-large-tv
```

Start with:

```text
sam-audio-large
```

for quality, plus Base for cost comparison.

Capabilities:

```text
TEXT_PROMPT_SEPARATION
TEMPORAL_PROMPT_SEPARATION
VISUAL_PROMPT_SEPARATION
OPEN_VOCAB_INSTRUMENT_EXTRACTION
```

Examples:

```text
"clarinet"
"acoustic guitar"
"accordion"
"shaker"
"background strings"
```

SAM Audio adds capability that BS-RoFormer does not have.

License:

```text
SAM License
```

not Apache/MIT.

Record it exactly.

---

# 15. UPGRADE MT3 FAMILY

Do not replace current MT3.

Extend current MT3 worker using `mt3-infer`.

Provider IDs:

```text
MT3_PYTORCH
MR_MT3
YOUR_MT3
```

Install current `mt3-infer` pinned.

The toolkit officially supports all three.

Pre-download:

```bash
mt3-infer download --all
```

but during provisioning only.

Current model sizes:

```text
MR-MT3       ~176 MB
MT3-PyTorch  ~176 MB
YourMT3      ~536 MB
```

YourMT3 provides 8-stem/multitask capabilities; MR-MT3 is optimized for speed.

Set:

```text
MT3_CHECKPOINT_DIR=/var/lib/music-ai/models/mt3
```

No runtime downloads.

Important licensing:

```text
MR-MT3 = MIT
YourMT3 = Apache-2.0
MT3-PyTorch wrapper/upstream license = NOASSERTION
```

Do not relabel MT3-PyTorch as Apache.

---

# 16. INSTALL MUSIC2MUSIC + REMI-z

This is one of the highest-value symbolic systems.

Provider:

```text
MUSIC2MUSIC
```

Tokenizer/library:

```text
REMI_Z
```

Install REMI-z:

```bash
pip install REMI-z
```

It provides:

```text
MIDI ↔ REMI-z
MultiTrack
Bar
Track
Note
bar-level instrumentation/control
```

Models:

```text
LongshenOu/m2m_pt
LongshenOu/m2m_arranger
LongshenOu/m2m_pianist_dur
LongshenOu/m2m_drummer
```

All are around 87M-class models.

Worker:

```text
services/music2music-worker/
```

Use Transformers-compatible inference.

However:

## LICENSE GATE

The public model cards do not provide a sufficiently explicit production license.

Therefore installation status:

```text
RESEARCH_ONLY_PENDING_LICENSE
```

Allowed:

```text
download for private research if terms permit
build worker
prepare adapter
benchmark
```

Not allowed:

```text
mark COMMERCIAL_READY
```

Pipeline:

```text
SongModel/MIDI
→ REMI-z
→ m2m arranger
→ MIDI
→ TrackModels
```

---

# 17. INSTALL METEOR

Keep this from original plan.

Provider:

```text
METEOR
```

Isolated:

```text
Python 3.9.2
```

Pin exact upstream commit and full requirements.

Pipeline:

```text
MIDI
→ analyzed MIDI
→ corpus
→ REMI/MuseMorphose
→ METEOR
→ reorchestrated MIDI
```

Do not merge with Music2Music.

These become two independent symbolic arrangement families.

---

# 18. INSTALL SYMPHONYGEN

Provider:

```text
SYMPHONYGEN
```

Worker isolated.

Keep current upstream requirements:

```text
torch==2.8.0
torchaudio==2.8.0
transformers==5.1.0
```

Download all official checkpoints:

```text
stage_one_pretrained.pt
stage_two_pretrained.pt
grpo_clamp_epoch_10.pt
grpo_clamp+track_epoch_6.pt
```

Role:

```text
ORCHESTRAL_SYMBOLIC_GENERATION
```

---

# 19. INSTALL MUSICGEN + JASCO

If AudioCraft worker is being installed anyway, install both.

Provider family:

```text
MUSICGEN_LARGE
MUSICGEN_MELODY_LARGE
JASCO_CHORDS_DRUMS_MELODY
```

AudioCraft runtime stays isolated:

```text
Python 3.9
Torch 2.1.0
```

JASCO is not for full-song primary generation.

Use it for:

```text
fills
transitions
short local repair
explicit melody/chord/drum conditioning
```

Do not create a separate JASCO worker if AudioCraft worker already exists.

---

# 20. INSTALL DIFFRHYTHM 2

Replace old plan of DiffRhythm 1.x with DiffRhythm 2.

Provider:

```text
DIFFRHYTHM_2
```

Source:

```text
xiaomi-research/diffrhythm2
```

Code + weights:

```text
Apache-2.0
```

Weights are publicly released.

Worker:

```text
services/diffrhythm2-worker/
```

Pin exact Git commit and requirements.

Do not allow first-run HF downloads.

Run upstream once in provisioning mode, capture:

```text
all downloaded repos
exact HF revisions
SHA256
```

Capabilities:

```text
full-song generation
lyrics-conditioned generation
reference-audio conditioning
```

---

# 21. LEVO 2 — INSTALL ONLY AS RESEARCH PROFILE

Provider:

```text
LEVO2_RESEARCH
```

Do not make commercial/production provider.

Reason:

```text
Tencent research/academic restrictions
```

Install only if license permits your current private research use.

Status:

```text
RESEARCH_ONLY
```

Do not let it enter production provider selection.

---

# 22. INSTALL MuQ-EVAL

Provider:

```text
MUQ_EVAL
```

Worker:

```text
services/muq-eval-worker/
```

Runtime:

```text
Python 3.11
CUDA 12.1-compatible Torch environment
```

Reference install:

```bash
pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu121
pip install -r requirements.txt
```

Recommended model:

```text
zhudi2825/MuQ-Eval-A1
```

A1 = Frozen MuQ + MLP.

Reported runtime:

```text
~3GB VRAM
~35ms per 10s clip
```

License:

```text
MIT
```

Output:

```text
musical impression / generated-music quality
```

Important:

It is one judge, not final truth.

---

# 23. INSTALL SONG AESTHETICS EVALUATION

Provider:

```text
SONG_AESTHETICS
```

Source:

```text
yisan33/song-aesthetics-evaluation
```

Code/inference/checkpoint are available.

Input expects aligned:

```text
mixture
vocal stem
accompaniment stem
```

Excellent fit because BS-RoFormer already provides these.

Outputs:

```text
Overall
Singing
Melody
Arrangement
AudioQuality
```

0–100.

Install environment from its provided `environment.yml`.

Pin all dependencies.

Provider role:

```text
MULTI_DIMENSIONAL_MUSIC_JUDGE
```

---

# 24. INSTALL SONGEVAL — RESEARCH ONLY

Provider:

```text
SONG_EVAL
```

Install official toolkit.

Capabilities:

```text
song aesthetics
coherence
memorability
musicality
etc.
```

License:

```text
CC BY-NC-SA 4.0
```

Therefore:

```text
RESEARCH_ONLY
```

Do not use as mandatory commercial runtime dependency.

---

# 25. MuQ / MuQ-MuLan — INSTALL AS SHARED EMBEDDING SERVICE

Do not make three competing embedding workers.

Create:

```text
MUSIC_EMBEDDINGS
```

with:

```text
MuQ
MuQ-MuLan
```

if licenses permit research use.

Purpose:

```text
reference-song similarity
style similarity
section similarity
candidate clustering
retrieval
duplicate detection
```

MuQ-Eval should reuse MuQ assets if compatible, but do not force environment sharing if version conflicts.

---

# 26. CLaMP 3 — OPTIONAL ADVANCED EMBEDDING WORKER

Provider:

```text
CLAMP3
```

Install only as separate optional semantic worker.

Capabilities:

```text
text ↔ audio
text ↔ MIDI
audio ↔ MIDI
score ↔ audio
```

Use for cross-modal validation.

Do not let CLaMP duplicate MuQ blindly.

Status:

```text
INSTALLED_EXPERIMENTAL
```

rather than default production dependency.

---

# 27. INSTALL sfizz + VSCO2 CE

Deterministic symbolic renderer remains necessary.

Install native:

```text
sfizz 1.2.3
```

Provision:

```text
VSCO2 CE
```

outside Git.

Directory:

```text
/var/lib/music-ai/assets/sfz/vsco2-ce
```

Record:

```text
tree SHA
files
bytes
license
```

Provider:

```text
SFIZZ_VSCO2_CE
```

---

# 28. FINISH VST3 HOST

Build:

```text
VST3_NATIVE_HOST
```

Must support:

```text
MIDI notes
velocity
CC
pitch bend
automation
plugin state
offline render
```

If no licensed VST3 instrument exists:

```text
VST3_HOST = INSTALLED
VST3_INSTRUMENT = BLOCKED_MISSING_LICENSED_ASSET
```

That is valid completion.

---

# 29. PROVIDER TAXONOMY

Final system should categorize providers.

## ANALYSIS

```text
ALL_IN_ONE
SONGFORMER
BEAT_THIS
MADMOM
ESSENTIA
CHROMA
MOSS_MUSIC
```

## TRANSCRIPTION

```text
MT3_PYTORCH
MR_MT3
YOUR_MT3
BASIC_PITCH
TORCHCREPE
SHEETSAGE
```

## SEPARATION

```text
BS_ROFORMER
DEMUCS
SAM_AUDIO
```

## V2A

```text
LADA_BAND
HAFM
ANYACCOMP
MIDI_SAG
```

## FULL AUDIO GENERATION

```text
ACE_STEP
STABLE_AUDIO_3
MUSICGEN
DIFFRHYTHM_2
LEVO2_RESEARCH
```

## CONTROLLED GENERATION

```text
MUSE_CONTROL_LITE
JASCO
MAGENTA_RT2
```

## SYMBOLIC ARRANGEMENT

```text
MUSIC2MUSIC
METEOR
SYMPHONYGEN
```

## RENDERING

```text
SFIZZ_VSCO2_CE
VST3_NATIVE
MAGENTA_RT2_GENERATIVE
```

## EVALUATION

```text
MUQ_EVAL
SONG_AESTHETICS
SONG_EVAL
PYLOUDNORM
```

---

# 30. WATCHLIST — DO NOT FAKE INSTALL

Create entries in:

```text
research-watchlist.json
```

for:

```text
S2Accompanist
BeatEdit
Live Music Diffusion Models
Diff-A-Riff
Break-the-Beat
```

For each:

```json
{
  "status": "BLOCKED_NO_INSTALLABLE_RELEASE",
  "paper": "...",
  "repository": "...",
  "weightsAvailable": false,
  "lastReviewed": "..."
}
```

Do not create Dockerfiles pretending these are installed.

---

# 31. COMMON PROVIDER CONTRACT

Every new provider gets:

```text
Dockerfile
requirements.lock / uv.lock
model_manifest.json
license_manifest.json
bootstrap_assets.py
app.py
inference.py
README.md
tests/
modal_config.py
modal_app.py
```

where relevant.

---

# 32. MANIFEST REQUIREMENTS

Every model must record:

```text
provider
modelVersion

codeRepository
codeRevision

modelRepository
modelRevision

checkpoint files
checkpoint SHA256
tree SHA256

Python
CUDA
Torch
Torchaudio
Torchvision
Transformers

code license
weights license
commercial status

asset bytes
source URLs

runner version
```

No `main`.

No `latest`.

---

# 33. DOWNLOAD POLICY

Production request handler is forbidden from downloading:

```text
GitHub source
HF model
Google Drive asset
checkpoint
tokenizer
codec
VAE
vocoder
```

All assets must be provisioned before deployment.

---

# 34. LICENSE STATUS ENUM

Use:

```text
COMMERCIAL_OK
RESEARCH_ONLY
LICENSE_REVIEW_REQUIRED
BLOCKED_LICENSE
BLOCKED_NO_WEIGHTS
```

Examples:

```text
MOSS-Music        COMMERCIAL_OK / Apache-2.0
Magenta RT2       COMMERCIAL_OK / CC-BY-4.0 weights
Beat This         COMMERCIAL_OK / MIT
DiffRhythm2       COMMERCIAL_OK / Apache-2.0
Stable Audio 3    LICENSED_COMMERCIAL_PATH
LaDA-Band         RESEARCH_ONLY
SongEval          RESEARCH_ONLY
Music2Music       LICENSE_REVIEW_REQUIRED
LeVo2             RESEARCH_ONLY
```

Do not infer commercial rights merely because source code has an open license.

---

# 35. GPU INITIAL DEPLOYMENT

Start generous.

Suggested initial classes:

```text
ACE_STEP              L40S
MOSS_MUSIC             L40S / A100-class if required
LADA_BAND              L40S
HAFM                   L40S
STABLE_AUDIO_3_MEDIUM  L40S
SONGFORMER             L4/L40S benchmark
SAM_AUDIO_LARGE        L40S
MAGENTA_RT2_BASE       L40S offline
DIFFRHYTHM2            L40S
MUSIC2MUSIC            L4
MUQ_EVAL               L4
```

Do not optimize cost in installation phase.

---

# 36. VOLUMES

Recommended:

```text
music-ai-models-v2/
  ace-step/
  lada-band/
  hafm/
  stable-audio3/
  diffrhythm2/
  moss-music/
  magenta-rt2/
  sam-audio/
  music2music/
  meteor/
  symphonygen/

music-analysis-models-v2/
  mt3/
  songformer/
  beat-this/
  sheetsage/
  muq/
  muq-eval/

music-assets-v2/
  sfz/
  vst3/
```

Do not duplicate huge checkpoints when safe sharing is possible.

---

# 37. SECRETS

Required inventory may include:

```text
HF_TOKEN
STABILITY_HF_TOKEN
LADA_BAND_HF_TOKEN
SAM_AUDIO_HF_TOKEN

SHEETSAGE_API_TOKEN
SHEETSAGE_LICENSE_SECRET

internal provider bearer tokens

promotion signing secrets
```

No secrets committed to Git.

---

# 38. API ENDPOINTS

Provide canonical environment keys:

```text
MOSS_MUSIC_API_URL
SONGFORMER_API_URL
BEAT_THIS_API_URL

LADA_BAND_API_URL
HAFM_API_URL

STABLE_AUDIO_3_API_URL
MAGENTA_RT2_API_URL
SAM_AUDIO_API_URL

MUSIC2MUSIC_API_URL
DIFFRHYTHM2_API_URL

MUQ_EVAL_API_URL
SONG_AESTHETICS_API_URL
SONG_EVAL_API_URL
```

Use aliases only for backward compatibility.

---

# 39. INSTALLATION ORDER

Execute in this order.

### Wave 1 — finish existing stack

```text
SheetSage
MIR workers
Basic Pitch
Demucs
current GPU workers
```

### Wave 2 — next-generation analysis

```text
Beat This
SongFormer
MOSS-Music Instruct
MOSS-Music Thinking
MR-MT3
YourMT3
```

### Wave 3 — direct accompaniment

```text
LaDA-Band
HAFM
AnyAccomp
MIDI-SAG
MuseControlLite
```

### Wave 4 — neural audio editing/generation

```text
Stable Audio 3 Small
Stable Audio 3 Medium
DiffRhythm 2
MusicGen
JASCO
```

### Wave 5 — open-vocabulary and real-time

```text
SAM Audio
Magenta RealTime 2 Small
Magenta RealTime 2 Base
```

### Wave 6 — symbolic intelligence

```text
REMI-z
Music2Music
METEOR
SymphonyGen
```

### Wave 7 — evaluation stack

```text
MuQ
MuQ-Eval
Song Aesthetics Evaluation
SongEval
CLaMP3 optional
```

### Wave 8 — rendering

```text
sfizz
VSCO2 CE
VST3 native host
```

---

# 40. FINAL INSTALLATION MATRIX

Create:

```text
installation-matrix-v2.json
```

Every provider must contain:

```json
{
  "provider": "",
  "category": "",
  "codePinned": false,
  "runtimeBuilt": false,
  "modelPinned": false,
  "assetsDownloaded": false,
  "assetsChecksummed": false,
  "volumeProvisioned": false,
  "licenseStatus": "",
  "secretsConfigured": false,
  "endpointDefined": false,
  "installed": false,
  "notes": []
}
```

---

# 41. DEFINITION OF DONE FOR THIS PHASE

Installation phase finishes only when:

```text
all installable source trees pinned
all environments built
all checkpoints downloaded
all checkpoints hashed
all model volumes populated
all license states recorded
all required secrets documented/configured
all endpoint definitions created
all provider adapters compiled/importable
no request-time downloads remain
```

Research-only providers may be marked:

```text
installed=true
licenseStatus=RESEARCH_ONLY
```

Models with no deployable public release:

```text
installed=false
status=BLOCKED_NO_WEIGHTS
```

Models with uncertain license:

```text
installed=true
status=LICENSE_REVIEW_REQUIRED
```

---

# 42. DO NOT DO DURING THIS TASK

Do NOT:

```text
run Golden Song
score candidates
rewrite Arrangement Director
rewrite HarmonyEngine
optimize GPU costs
polish UI
add random new models
create fake smokes
replace existing working models
```

This task ends when the entire known model universe we selected is physically provisioned and catalogued.

---

# FINAL TARGET STACK

The final installed research/production platform should contain:

```text
ACE-Step
BS-RoFormer
Demucs
SAM Audio

All-In-One
SongFormer
Beat This
Madmom
Essentia
Chroma
SheetSage
TorchCREPE
Basic Pitch

MT3-PyTorch
MR-MT3
YourMT3

MOSS-Music Instruct
MOSS-Music Thinking

LaDA-Band
HAFM
AnyAccomp
MIDI-SAG
MuseControlLite

Stable Audio 3 Small
Stable Audio 3 Medium
MusicGen
JASCO
DiffRhythm 2
LeVo2 Research

REMI-z
Music2Music
METEOR
SymphonyGen

Magenta RealTime 2 Small
Magenta RealTime 2 Base

MuQ
MuQ-Eval
Song Aesthetics Evaluation
SongEval
CLaMP3

pyloudnorm
Pedalboard

sfizz
VSCO2 CE
VST3 native host
```

Watchlist:

```text
S2Accompanist
BeatEdit
Live Music Diffusion Models
Diff-A-Riff
Break-the-Beat
```

---

# END STATE

לאחר סיום המסמך הזה אנחנו צריכים להיות מסוגלים לומר:

> “אין כרגע מודל או ספרייה משמעותיים שהחלטנו שהם רלוונטיים לפרויקט ועדיין לא provisioned.”

רק אז עוברים לשלב הבא:

**Real inference certification → Golden Song → model shootout → musical evaluation → Arrangement Director V2 → professional production quality.**
